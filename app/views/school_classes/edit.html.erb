<%= form_for @school_class do |sc| %>
<%= sc.label 'School Class Title' %><br>
<%= sc.text_field :title, @school_class.title %>
<%= sc.label 'School Class Room Number' %><br>
<%= sc.number_field :room_number, @school_class.room_number %>
<%= sc.submit "Update School Class"%>
<% end %>